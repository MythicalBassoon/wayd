
module.exports = {
	getEvents: (req, res) => {
		console.log("in controller, with ES6!");
		//make get request eventful api
		//passing in location which should be contained in req.body.params
		//capture random event
		//return res.json(events)
	}
}